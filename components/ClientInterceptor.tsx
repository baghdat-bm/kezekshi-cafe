"use client";

import "../lib/axiosInterceptor";

export default function ClientInterceptor() {
    // Этот компонент не рендерит ничего, он лишь гарантирует, что interceptor инициализирован
    return null;
}
